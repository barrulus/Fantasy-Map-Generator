# Upstream sync 1.153.x — fork main ← Azgaar master bf32ac72

Branch `sync/upstream-1.153.1`, worktree `.claude/worktrees/sync-1153` (clean `npm ci`).
Base: fork main 65bd51ff (PR #81), upstream master bf32ac72 (v1.153.0 #1856 + 1.153.1 #1877), merge-base f6381d94 (the 1.152.1 sync point).
~40 upstream commits, 31 conflicted paths (1 modify/delete), 342 files in the merge.
Note: upstream tagged 1.153.1 but `VERSION` and package.json still say 1.153.0; kept as upstream has it.

## What upstream did that shaped the resolution
- v1.153.0: Omnibar (`MAP_COMMANDS` replaces the tools-tab click ladder), Wrap Tool (replaces per-vertex coastline/lake editing, `CoastlineVertexEditor` is gone), coastline generator rewrite (per-feature settings, `shapes` cache, `getFeatureOutline`/`getFeatureShape`), Performance settings (`options.app.performance.{shapeRendering,stateHalos,viewportRedraw}` + presets quality/balance/speed; the two Options-tab rows moved into the Performance dialog), Ink/Cinderwood/Frostbite presets, `burg-icon-picker` element replacing the icon `<select>`, heightmap editor drainage overlay with `EDITOR_OPTIONS`, dialogs titlebar reset button, cascading table sorting, memory-leak fixes (`destroyDialog`, nullable heightmap-selection state, `removeGoods` layer teardown), `findDeepDepressionLakes` split out of `addDeepDepressionLakes`.
- Notes: `generators/notes.ts` (NoteRef, NotesStore with entity lookups) became `components/notes.ts` (store only) + `components/map-entities.ts` (`EntityRef`, `MapEntities.parseKey/resolveElement/key/getName/get`).
- `index.html`: the `#fileInputs` div is gone; inputs are created per module with `createFileInput`.
- PWA: `public/sw.js` is the precache-everything worker from #1833 (barrulus's own upstream PR), so the fork's older immutable/NetworkFirst routes are superseded — upstream's file taken wholesale (the fork delta was only those routes and a duplicate skipWaiting).
- 1.153.1 merged #1854 (diplomacy removed states), #1855 (anchor/burg icon style records), #1826, #1842 — fork worktrees for those are now redundant.

## Resolutions
- **README / Changelog / Policy / Quick-Start**: fork versions kept. Changelog "unreleased" list trimmed to the fork-only entry (This-map assistant); Policy "Turning it off" path and Quick-Start "On load" wording follow upstream's renamed settings.
- **versioning.ts**: upstream's list + "Journey Editor and new Journeys layer" (fork).
- **nix/package.nix**: fork hash kept; re-pin only from CI's "got:" line.
- **options-model.ts**: upstream's `performance` object, but `viewportRedraw: "settled"` = the **speed** preset (fork default "After zoom"). `performance.test.ts` and `zoom-redraw-option.spec.ts` expect `speed`/`settled`.
- **options-tab.ts**: rendering/redraw rows dropped (upstream moved them). Map wheel Behaviour rows: `shapeRendering`/`viewportRedraw` → `performancePreset`.
- **style-tab.ts**: `burg-icon-picker` taken; the fork's `diamond` icon added to its Atlas group.
- **tools-tab.ts**: `MAP_COMMANDS` dispatch taken; fork command `openAiChatButton` ("Ask AI: This Map") added to map-commands.ts next to `helpAssistant`. `overviewJourneysButton`/`editTradeAnimationButton` were already upstream.
- **help-assistant.ts**: fork's mode-aware reset button + upstream's `.ui-dialog-titlebar-reset` anchor.
- **notes-editor.ts / help-assistant-notes.ts**: fork bridge (`current/write/remove/getSelectionHtml`, `open(ref | key | element id)`) ported to `MapEntities.*`; `Notes.exists` → `MapEntities.get`. Upstream's `openAiGenerator` dropped: the fork's "Generate with AI" button opens the This-map assistant. `exportCsv` added to the export.
- **markers/military/regiments overviews, diplomacy, states editors**: upstream's `destroyDialog` teardown; `fitContent` stays where the fork still uses it.
- **heightmap-editor.ts**: upstream's `EDITOR_OPTIONS` loop + drainage; fork draft save/load re-added with a `createFileInput(".heightmap")` (id `heightmapDraftToLoad`) since the html input is gone.
- **heightmap-selection.ts**: upstream's nullable `graph`/`graphConfig` + fork's capped preview grid as `previewGraphConfig` (10K cells); the Generate button still passes the full config and no graph when the preview was capped.
- **coastline-generator.ts**: upstream's rewrite + the fork's `paths` cache, `boundaryRevision` and `restorePaths` (load.ts adopts saved display geometry; burg-water-survey reads the revision). `sourceKey` now includes `feature.coastline` so per-feature settings invalidate the cached path.
- **grid-generator.ts**: upstream's `findDeepDepressionLakes(cells, limit)` shape with the fork's priority-flood spillpoint algorithm inside, and the fork's land-only shore rule in `addDeepDepressionLakes`. Fork's batched lake-breach remap auto-merged.
- **styles-legacy.ts**: route type sub-group selector (`#routes > g#group > g#type`) + upstream's `#lakes > g#group`.
- **draw-burg-icons.ts**: fork viewport renderer kept; upstream's anchor icon-from-style and dx/dy offset ported (`anchorPlacement`). Upstream's jsdom test for its own renderer dropped again.
- **draw-goods.ts**: fork renderer + upstream's `removeGoods` teardown (layers.ts `erase`).
- **label-data.ts**: upstream's `build(geometry)`/`getLabelsIndex` + megalopolis composites on the burg labels.
- **sorting.test.ts / states-generator.test.ts**: both sides' tests kept.
- **map-wheel/context.ts**: "Edit coastline" (vertex editor) → "Wrap coastline" (`Controllers.WrapTool.open()`).
- **Presets**: ink/cinderwood/frostbite gained `routes.types` (widths scaled by roads width / 1.4) and `airroutes`/`traderoutes` groups derived from their searoutes/roads colours.
- package.json: version 1.153.0; `flatqueue` and `generate:agent-context` kept; no dependency lost (audited by name against fork main and upstream). Lock auto-merged and `npm ci` passed.
- Assets restamped; agent context regenerated.

## Verification
- tsc 0 errors; Biome clean (2 pre-existing warnings); `stamp-assets --check` and `sync-version --check` clean.
- vitest 1839/1839 (154 files).
- Fork-identifier audit (2600 ids added since the base): only sw.js comment words and changelog wording vanished, all deliberate.
- Production build OK (583 files precached); e2e run against `vite preview` (CI=1 SKIP_BUILD=1): see below.

## e2e (2026-09-16, production preview via `CI=1 SKIP_BUILD=1 CHROMIUM_PATH=/run/current-system/sw/bin/chromium npx playwright test`)
20 failed / 280 passed / 1 skipped (301 tests, 7 min).
Baseline set from the 1.152.1 ledger still failing (14): layers.spec snapshots ×11, style-parity (preset-apply baseline predates route types: `#roads`/`#trails` carry type widths), help-assistant-ai gateway ×2.
Baseline failures now passing: annex-mode "refuses to annex a province from a different state", burgs port toggle ×2.

New failures (6), diagnosed with headless probes against `vite preview` (system chromium):
- **annex-mode › State and province refresh › recolour … ×4**: seed `state-refresh` now generates three provinces (85–87, all in state 27) whose capitals are flying sky burgs. A sky burg's cell is never assigned to its province, so the province owns no cells and gets no `#provinceN` path, and upstream's test asserts every live province has one. Fork-feature interaction (the provinces generator does not exclude flying burgs) exposed by upstream's changed generation (precipitation/temperature/lakes edits move burg placement). Options: skip flying burgs when seeding provinces, or exempt cell-less provinces in the test.
- **burg-editor-open › by clicking a materialized burg icon**: the first on-screen icon (burg36, seed `icon-viewport`, zoom 3) sits under the state label "Zalban Empire" `tspan`, so the click opens the label editor. Label geometry moved with upstream's label styling; product behaviour (labels above icons) is unchanged. Test fragility: pick an icon not covered by a label.
- **styles-demo › every styles address resolves in the generated map's DOM**: schema key `routes/types` has no `[data-group="types"]` element. The fork stamps route types as `data-type` sub-groups inside each group (verified live: roads/royal, roads/main, roads/market, trails/trail, trails/footpath, searoutes/feeder, searoutes/coastal; the store has 8 types). Not a feature loss; the address walker treats `types` like a static sub-layer now that `#routes` carries `data-layer`. One-line option: skip `types` alongside `groups` in the spec.

None of the three are changed on this branch: they are findings for a separate decision, not merge resolutions.

## Fixes after the merge (same PR, separate commit)
- provinces-generator: flying burgs are no longer province seeds (a sky burg owns no ground cells, so its province had no cells and no path). annex-mode "State and province refresh" ×4 green.
- burg-editor-open.spec: the clicked icon must be the topmost element at its centre (`elementFromPoint`), not merely on screen.
- styles-demo.spec: `types` is skipped like `groups`; route types are `data-type` sub-groups inside each route group.
Verified: the three specs 15/15 on the rebuilt preview; vitest full run green; tsc and Biome clean.
